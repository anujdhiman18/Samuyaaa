rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Public read access for images (faculty, alumni, student photos, gallery)
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
