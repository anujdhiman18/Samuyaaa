rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Public read access for all collections (students, faculty, alumni, courses, testimonials, notices, etc.)
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
